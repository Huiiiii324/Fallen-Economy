# Sell Values

Sell values are paid in `$`.

Data file:

```text
plugins/FallenEconomy/sell-values.yml
```

Commands:

```text
/sell
/sell hand
/sell all
/sell values
```

`/sell all` scans the main storage inventory and hotbar. Armor and offhand are not sold.
